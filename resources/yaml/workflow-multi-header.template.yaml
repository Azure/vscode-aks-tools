# This workflow was generated by the AKS VS Code Extension (multi-container).
#
# 🔐 IMPORTANT: OIDC Authentication Required!
# Configure the following GitHub repository secrets before running:
#    - AZURE_CLIENT_ID
#    - AZURE_TENANT_ID
#    - AZURE_SUBSCRIPTION_ID
#
name: { { WORKFLOWNAME } }

on:
    push:
        branches: [{ { BRANCHNAME } }]
    workflow_dispatch:

env:
    ACR_RESOURCE_GROUP: { { ACRRESOURCEGROUP } }
    AZURE_CONTAINER_REGISTRY: { { AZURECONTAINERREGISTRY } }
    CLUSTER_NAME: { { CLUSTERNAME } }
    CLUSTER_RESOURCE_GROUP: { { CLUSTERRESOURCEGROUP } }
    NAMESPACE: { { NAMESPACE } }

jobs:
